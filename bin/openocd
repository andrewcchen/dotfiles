#!/bin/bash

device=${0##*.}
case $device in
$0)
	exec /usr/bin/openocd "$@"
	;;
stm32f[04])
	exec openocd -f interface/stlink-v2.cfg -f target/${device}x.cfg "$@"
	;;
*)
	echo >&2 "Unrecognized device $device"
	;;
esac
